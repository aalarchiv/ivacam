//! wiaConstructor desktop: Tauri 2 shell wrapping the Svelte frontend.

fn main() -> anyhow::Result<()> {
    eprintln!("wiac-desktop: Tauri integration pending (see bd epic wiaconstructor-u40)");
    Ok(())
}
