"""Stage-1 FastAPI bridge for wiaConstructor.

Wraps the existing Python viaConstructor importer + (later) CAM math behind
the JSON contract defined in `schema/openapi.yaml`. This is throwaway
scaffolding so the web frontend can ship before the Rust port lands.
"""

__version__ = "0.0.1"
